steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'asia-southeast2-docker.pkg.dev/$PROJECT_ID/sicakra/sicakra-user:$SHORT_SHA'
      - '-t'
      - 'asia-southeast2-docker.pkg.dev/$PROJECT_ID/sicakra/sicakra-user:latest'
      - '.'

  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - '--all-tags'
      - 'asia-southeast2-docker.pkg.dev/$PROJECT_ID/sicakra/sicakra-user'

  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'sicakra-user'
      - '--image=asia-southeast2-docker.pkg.dev/$PROJECT_ID/sicakra/sicakra-user:$SHORT_SHA'
      - '--region=asia-southeast2'
      - '--platform=managed'
      - '--allow-unauthenticated'
      - '--port=3003'
      - '--memory=512Mi'
      - '--cpu=1'
      - '--min-instances=0'
      - '--max-instances=3'
      - '--set-secrets=NEXT_PUBLIC_API_URL=sicakra-api-url:latest'

images:
  - 'asia-southeast2-docker.pkg.dev/$PROJECT_ID/sicakra/sicakra-user'

options:
  logging: CLOUD_LOGGING_ONLY
